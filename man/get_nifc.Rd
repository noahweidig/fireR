% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_nifc.R
\name{get_nifc}
\alias{get_nifc}
\title{Download and unzip NIFC wildfire data}
\usage{
get_nifc(
  directory = getwd(),
  overwrite = FALSE,
  timeout = 3600,
  verbose = TRUE,
  dry_run = FALSE
)
}
\arguments{
\item{directory}{\code{character(1)} directory where the ZIP file and
unzipped contents are stored.  Defaults to the current working directory.}

\item{overwrite}{\code{logical(1)} re-download when \code{TRUE};
defaults to \code{FALSE}.}

\item{timeout}{\code{numeric(1)} download timeout in seconds.
Defaults to \code{3600} (one hour).}

\item{verbose}{\code{logical(1)} print progress messages.}

\item{dry_run}{\code{logical(1)} if \code{TRUE}, do not download the file but instead return the path where it would be saved. Defaults to \code{FALSE}.}
}
\value{
\code{character(1)} path to the downloaded ZIP file (invisibly).
}
\description{
Downloads the NIFC (National Interagency Fire Center) wildfire perimeters
dataset from figshare and unzips it to a local directory.  If the ZIP
already exists and \code{overwrite = FALSE}, no network call is made.
}
\section{Warning [large files]}{

The NIFC ZIP archive is a large file. Downloads may be slow depending
on connection speed. Consider downloading in a background session if needed.
}

\examples{
\dontrun{
zip_path <- get_nifc()
zip_path <- get_nifc(directory = "data/nifc")
}
}
