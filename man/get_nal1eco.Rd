% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_eco.R
\name{get_nal1eco}
\alias{get_nal1eco}
\title{Download and load North America Level 1 Ecoregions}
\usage{
get_nal1eco(
  output = c("sf", "vect", "terra"),
  cache = FALSE,
  timeout = 3600,
  verbose = TRUE,
  dry_run = FALSE,
  overwrite = FALSE
)
}
\arguments{
\item{output}{\code{character(1)} class of the returned spatial object.
Either \code{"sf"} (default) or \code{"vect"} / \code{"terra"} for a
\code{terra::SpatVector}.}

\item{cache}{\code{logical(1)} or \code{character(1)}.  Controls where the
ZIP is cached.  \code{FALSE} (the default) uses a temporary directory that
is cleaned up when the R session ends.  \code{TRUE} uses the platform user
cache directory (\code{tools::R_user_dir("fireR", "cache")}).  Supply a
directory path as a string to specify a custom location.}

\item{timeout}{\code{numeric(1)} download timeout in seconds. Default is \code{3600}.}

\item{verbose}{\code{logical(1)} print progress messages.}

\item{dry_run}{\code{logical(1)} if \code{TRUE}, do not download the file but instead return the path where it would be saved. Defaults to \code{FALSE}.}

\item{overwrite}{\code{logical(1)} re-download when \code{TRUE}, deleting any
cached ZIP first; defaults to \code{FALSE}.}
}
\value{
An \code{sf} object or \code{terra::SpatVector} of North America
Level 1 Ecoregion polygons. (Or the file path invisibly if \code{dry_run = TRUE}).
}
\description{
Downloads and loads the Commission for Environmental Cooperation (CEC)
North America Level 1 Ecoregion boundaries as a spatial object.
}
\section{About CEC Ecoregions}{

The Commission for Environmental Cooperation (CEC) North American ecoregion
framework divides the continent into hierarchical levels based on similarity
of ecosystems and the type, quality, and quantity of environmental
resources.  Level 1 represents the broadest ecological divisions of the
continent.  Data are sourced from the US EPA / CEC ecoregion mapping
programme.
}

\examples{
\dontrun{
eco <- get_nal1eco()
eco_terra <- get_nal1eco(output = "vect")
}
}
