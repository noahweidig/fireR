Files changed:
- R/get_mtbs.R: Added `@section Warning \[large files\]:` to `get_mtbs` to warn about large 360MB download, matching SE FireMap and WUI pattern.
- man/get_mtbs.Rd: Generated via `roxygen2` corresponding to the R code update.
- README.md: Moved the MTBS warning block from "About MTBS" to the top of the "Usage" section to make it more prominent for users.

Why it was safe:
- These are purely documentation changes. They do not alter function signatures, behavior, architecture, or dependencies.
- Changes match the existing conventions inside the repository.

Validation:
- `devtools::document()`: successfully regenerated `man/` files without errors using roxygen2 8.0.0.
- `devtools::test()`: All tests passed (0 errors, 0 failures, 16 safely skipped due to network checks).
- `rcmdcheck::rcmdcheck()`: Passed with 0 errors, 0 warnings, 0 notes.
- `pkgdown::build_site(new_process = FALSE)`: successfully built the pkgdown site. Note: `docs/` artifacts from pkgdown and `R CMD build` were cleaned up before committing to prevent bloat.
