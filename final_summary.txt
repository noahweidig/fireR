Changed files:
- tests/testthat/test-get_eco.R: Added missing test case for `get_usl3eco(verbose = NA)` parameter validation logic, as requested in the audit report.

Why it is safe:
- It only modifies tests, preventing any regressions in package behavior.
- It covers an edge case directly mentioned as missing in the audit.
- No defaults, arguments, architecture, or configurations were modified, strictly following the constraints.

Validation results:
- devtools::test() passed (0 errors/warnings).
- devtools::document() successfully updated the documentation (no changes needed since it's just tests).
- rcmdcheck::rcmdcheck() passed with 0 errors/warnings/notes.
- pkgdown::build_site() built successfully.
