Changed Files:
- README.md: Added explicit argument tables for `read_nifc()` and `read_fod()` functions.

Why each change was safe:
- The changes to README.md are purely documentation updates. They bring the documentation for `read_nifc()` and `read_fod()` in line with the existing table structure for `read_mtbs()`, making it easier for users to reference the function arguments. No code, logic, or dependencies were altered, ensuring complete backward compatibility.

Validation:
- `devtools::document()`: Passed (regenerated documentation without errors).
- `devtools::test()`: Passed (all 264 tests passed, 0 failures, 16 safely skipped due to network checks).
- `rcmdcheck::rcmdcheck(args = c("--no-manual", "--compact-vignettes=gs+qpdf"))`: Passed (0 errors, 0 warnings, 0 notes).
- `pkgdown::build_site(new_process = FALSE)`: Passed (built the pkgdown site successfully).
