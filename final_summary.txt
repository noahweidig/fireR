## Final Summary

### Changed Files:
- `tests/testthat/test-get_eco.R`: Added test cases for validating the `verbose` argument in `get_nal2eco()`, `get_nal3eco()`, and `get_usl4eco()`, as well as validating the `state` argument in `get_usl4eco()`.

### Safety Rationale:
The changes made only involve adding missing tests for argument validation, which are internal to the test suite and do not affect the public API or the package's functionality for users. Adding these tests brings consistency across the `get_eco.R` testing suite, ensuring proper validation is in place without altering any default behavior, introducing dependencies, or triggering large downloads.

### Validation Results:
- `devtools::test()`: All tests passed (235 PASS, 16 SKIP, 0 FAIL, 0 WARN), taking 136.1 seconds.
- `rcmdcheck::rcmdcheck(args = c("--no-manual", "--compact-vignettes=gs+qpdf"))`: Package build and check completed successfully with 0 errors, 0 warnings, and 0 notes.
## Changed Files:
- `audit_report.md`: Updated to correctly document the missing tests for `verbose` and `state` validation in `test-get_eco.R`.
- `tests/testthat/test-get_eco.R`: Added missing `expect_error` checks for the `verbose` argument in `get_nal2eco`, `get_nal3eco`, and `get_usl4eco`, as well as checks for the `state` argument in `get_usl4eco`.
- `tests/testthat/test-get_mtbs.R`: Added missing `expect_error` checks for the `geometry` argument in `read_mtbs` (for `NA` and `numeric` cases, aligning it with `read_nifc` and `read_fod`).

## Why Each Change was Safe:
- The changes strictly involve adding new `testthat` assertions to improve test coverage for argument validation.
- No package logic, function arguments, or default behaviors were changed, ensuring 100% backward compatibility.
- The `state` argument in `get_usl4eco` is indeed a logical parameter (`TRUE`/`FALSE`) representing whether to include state boundaries, so the `expect_error(..., "TRUE.*FALSE")` regex is correctly matched to the actual error message generated.
- The `geometry` argument checks in `test-get_mtbs.R` check valid scenarios that were already safeguarded in code but previously missing test coverage.
## Final Summary

Changed Files:
- `tests/testthat/test-get_eco.R`

Why it is safe:
This change only adds comprehensive unit tests verifying that `get_nal1eco()`, `get_nal2eco()`, `get_nal3eco()`, `get_usl3eco()`, and `get_usl4eco()` correctly reject all invalid shapes of the `timeout` argument (strings, negatives, `NA`, and multi-element vectors). No implementation logic or defaults were changed. No network-heavy operations were added to the test suite.

Validation results:
- `devtools::document()`: Passed (no changes to roxygen required)
- `devtools::test()`: Passed (250 tests OK, including the newly added tests)
- `rcmdcheck::rcmdcheck(args = c("--no-manual", "--compact-vignettes=gs+qpdf"))`: Passed (0 errors, 0 warnings, 0 notes)
- `pkgdown::build_site(new_process = FALSE)`: Passed (site built successfully)
