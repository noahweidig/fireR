## Changed Files:
- `audit_report.md`: Updated to correctly document the missing tests for `verbose` and `state` validation in `test-get_eco.R`.
- `tests/testthat/test-get_eco.R`: Added missing `expect_error` checks for the `verbose` argument in `get_nal2eco`, `get_nal3eco`, and `get_usl4eco`, as well as checks for the `state` argument in `get_usl4eco`.

## Why Each Change was Safe:
- The changes strictly involve adding new `testthat` assertions to improve test coverage for argument validation.
- No package logic, function arguments, or default behaviors were changed, ensuring 100% backward compatibility.
- The `state` argument in `get_usl4eco` is indeed a logical parameter (`TRUE`/`FALSE`) representing whether to include state boundaries, so the `expect_error(..., "TRUE.*FALSE")` regex is correctly matched to the actual error message generated.

## Validation Passed:
- `devtools::document()`: Ran successfully. No `.Rd` file changes were necessary because only tests were modified.
- `devtools::test()`: Tests ran successfully with 0 failures, 0 warnings.
- `rcmdcheck::rcmdcheck(args = c("--no-manual", "--compact-vignettes=gs+qpdf"))`: Finished successfully with 0 errors, 0 warnings, 0 notes.
- `pkgdown::build_site(new_process = FALSE)`: Successfully updated the documentation website structure without errors.
